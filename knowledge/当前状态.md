# 当前状态

## 已完成
- 方向一 A1-A5 全部实验产出（A1 五图投票 KRR=0.986、异人 FAR=0.000；A2 噪声鲁棒；A3 端到端+熔断；A4 性能；A5 攻击+隐私）
- 方向二 B0-B4 全部实验产出（准入 p50≈680ms；601 次攻击 100% 拦截；流量整形熵增益）
- 方向三 C0-C4 全部实验产出（200 DID 零冲突；审计贯通率 1.0；7 维安全矩阵；C3 10 类攻击全拦截）
- 方向三安全闭环：上游方向二 Session Credential 模拟交接 → KDC 原子签发双 ST（绑定 pair_id/user_did/agent_did/parent_ticket_id）→ MCP 服务端跨字段全验证
- 三方向单测全部通过（方向一 26、方向二 25、方向三 25）

## 已知问题 / 待办
- 【重要·数据失真】性能对比数据暂不可直接用于论文（gmalg 纯 Python 所致，已分析定论：无需重跑，改口径即可）：
  - A4 性能对比不公平：国密(gmalg/Python) vs 对标(cryptography/C)，SM3≈800×、SM4≈200×、SM9 sign≈244×/verify≈9000× 的“慢倍数”主要是实现语言差异，非算法本身差异
  - C2 性能对比不可用：ours qps≈1（gmalg + GIL + 加锁串行）vs oauth qps≈15万（cache_auth_state 纯模拟基线）
  - C2 公式推导已验证（与实测吻合）：每请求 4 sign + 6 verify（obtain_tickets 含上游 Session Credential 验证 2 签 + 双签名链 2 签 + 服务端 6 验）；C2 为 Python 本地线程/loopback 微基准，非生产/公网性能，无需重跑
  - 处理方案（不重跑）：C2 改为“每请求 8 次 SM9 运算”结构化推导 + 附“若换 C 实现(≈0.5ms/次)则 qps≈250”的扩展预期；A4 分栏标注实现语言差异或补 C 库 benchmark 同口径参考
- 方向一 2 个测试期望待同步（max_correct=28、AUC 方向）
- 三方向数据面 / NAT / MCP / OAuth 均为模拟，论文需如实标注 simulated
- 端到端实验 H（三方向串接）尚未实现

## 环境
- Python 3.14 + venv；gmalg 1.1.2 纯 Python（时延仅趋势性对比）
- GPU/CUDA 沙箱不稳定，方向一人脸特征提取回退 CPU 多进程
- 固定种子 SEED=20260817，全流程可复现
